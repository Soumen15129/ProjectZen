# ── Web framework ──────────────────────────────────────────────────────────────
# Version floors (not exact pins) — claude-agent-sdk's mcp dependency needs
# anyio>=4.5, which the old fastapi==0.111.0 pin's starlette (anyio<5,>=3.4.0
# — compatible, but only with a newer starlette than 0.111.0 bundles) can't
# satisfy at that exact old version.
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
# app.py imports pydantic directly for its request models, so declare it rather
# than relying on fastapi pulling it in transitively.
pydantic>=2.0

# ── Claude Agent SDK (runs via the local `claude` CLI, authenticated against ──
# ── the user's Claude Enterprise seat — no ANTHROPIC_API_KEY) ──────────────────
claude-agent-sdk>=0.2.0

# ── LangGraph + LangSmith (agent orchestration & observability) ────────────────
langgraph>=0.2.0
langsmith>=0.1.0

# ── Database ───────────────────────────────────────────────────────────────────
aiosqlite==0.20.0

# ── Document generation ────────────────────────────────────────────────────────
openpyxl==3.1.2
python-docx==1.1.2
python-pptx==0.6.23
reportlab==4.2.0

# ── Document extraction ────────────────────────────────────────────────────────
mammoth==1.8.0
pdfminer.six>=20221105
pypdf>=4.0.0
lxml>=5.3.0

# ── Async file I/O ─────────────────────────────────────────────────────────────
aiofiles==23.2.1
